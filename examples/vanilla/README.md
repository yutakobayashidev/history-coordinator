# Vanilla Example

`@path-controller/core` をブラウザの素の DOM から試すためのサンプルです。

## 使い方

```bash
pnpm install
pnpm --filter @path-controller/core build
pnpm --filter @path-controller/example-vanilla dev
```

開発サーバーが `http://localhost:5173` で起動し、履歴操作のイベントが画面に表示されます。
